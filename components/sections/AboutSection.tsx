'use client'\n\nimport { motion } from 'framer-motion'\nimport Image from 'next/image'\n\nconst stats = [\n  { label: 'Episodes Published', value: '150+' },\n  { label: 'Guests Featured', value: '120+' },\n  { label: 'Countries Reached', value: '45+' },\n  { label: 'Community Members', value: '50K+' },\n]\n\nexport function AboutSection() {\n  return (\n    <section id=\"about\" className=\"section-padding bg-dark-bg relative overflow-hidden\">\n      <div className=\"container-custom space-y-16\">\n        {/* Our Story */}\n        <motion.div\n          initial={{ opacity: 0, y: 20 }}\n          whileInView={{ opacity: 1, y: 0 }}\n          transition={{ duration: 0.6 }}\n          viewport={{ once: true }}\n          className=\"space-y-8\"\n        >\n          <div className=\"space-y-4\">\n            <motion.span\n              className=\"text-gold text-sm font-semibold uppercase tracking-widest inline-block\"\n              initial={{ opacity: 0 }}\n              whileInView={{ opacity: 1 }}\n              transition={{ delay: 0.2 }}\n              viewport={{ once: true }}\n            >\n              About Us\n            </motion.span>\n            <h2 className=\"font-display text-4xl md:text-5xl font-black\">Our Story</h2>\n          </div>\n\n          <div className=\"grid md:grid-cols-2 gap-12 items-center\">\n            <motion.div\n              className=\"relative h-96 rounded-2xl overflow-hidden glassmorphism-dark\"\n              initial={{ opacity: 0, scale: 0.95 }}\n              whileInView={{ opacity: 1, scale: 1 }}\n              transition={{ delay: 0.2 }}\n              viewport={{ once: true }}\n            >\n              <img\n                src=\"https://images.unsplash.com/photo-1598488035139-bdbb2231ce04?w=600&h=600&fit=crop\"\n                alt=\"Ama Wacha Tu Studio\"\n                className=\"w-full h-full object-cover\"\n              />\n            </motion.div>\n\n            <motion.div\n              className=\"space-y-6\"\n              initial={{ opacity: 0, x: 20 }}\n              whileInView={{ opacity: 1, x: 0 }}\n              transition={{ delay: 0.3 }}\n              viewport={{ once: true }}\n            >\n              <p className=\"text-lg text-white/80 leading-relaxed\">\n                Ama Wacha Tu was born from a simple idea: to create a space where real people share real conversations. In a world full of polished narratives and curated content, we believe in the power of authentic dialogue.\n              </p>\n              <p className=\"text-lg text-white/80 leading-relaxed\">\n                What started as a passion project has grown into a thriving community of listeners, speakers, and thinkers across Africa. Our mission is to amplify diverse voices and create meaningful connections through honest conversations.\n              </p>\n              <p className=\"text-lg text-white/80 leading-relaxed\">\n                From entrepreneurship to mental health, from relationships to personal growth, we explore the topics that matter most to our community. Every episode is a journey, every guest a story waiting to be told.\n              </p>\n            </motion.div>\n          </div>\n        </motion.div>\n\n        {/* Core Values */}\n        <motion.div\n          className=\"space-y-8\"\n          initial={{ opacity: 0, y: 20 }}\n          whileInView={{ opacity: 1, y: 0 }}\n          transition={{ duration: 0.6, delay: 0.2 }}\n          viewport={{ once: true }}\n        >\n          <div className=\"space-y-4\">\n            <h3 className=\"font-display text-3xl md:text-4xl font-black\">Our Values</h3>\n          </div>\n\n          <div className=\"grid md:grid-cols-3 gap-6\">\n            {[\n              { title: 'Authenticity', desc: 'We believe in real conversations and genuine connections.' },\n              { title: 'Inclusivity', desc: 'Every voice matters. We celebrate diversity in all forms.' },\n              { title: 'Impact', desc: 'Our goal is to inspire positive change and personal growth.' },\n            ].map((value, index) => (\n              <motion.div\n                key={value.title}\n                className=\"glassmorphism-dark p-8 rounded-xl space-y-3\"\n                initial={{ opacity: 0, y: 10 }}\n                whileInView={{ opacity: 1, y: 0 }}\n                transition={{ delay: index * 0.1 }}\n                viewport={{ once: true }}\n              >\n                <h4 className=\"font-display text-2xl font-bold text-gold\">{value.title}</h4>\n                <p className=\"text-white/70\">{value.desc}</p>\n              </motion.div>\n            ))}\n          </div>\n        </motion.div>\n\n        {/* Statistics */}\n        <motion.div\n          className=\"space-y-8\"\n          initial={{ opacity: 0, y: 20 }}\n          whileInView={{ opacity: 1, y: 0 }}\n          transition={{ duration: 0.6, delay: 0.4 }}\n          viewport={{ once: true }}\n        >\n          <div className=\"space-y-4\">\n            <h3 className=\"font-display text-3xl md:text-4xl font-black\">By The Numbers</h3>\n          </div>\n\n          <div className=\"grid md:grid-cols-4 gap-6\">\n            {stats.map((stat, index) => (\n              <motion.div\n                key={stat.label}\n                className=\"text-center p-6 rounded-xl border border-gold/20 hover:border-gold/50 transition-colors\"\n                initial={{ opacity: 0, scale: 0.8 }}\n                whileInView={{ opacity: 1, scale: 1 }}\n                transition={{ delay: index * 0.1 }}\n                viewport={{ once: true }}\n              >\n                <motion.div\n                  className=\"text-4xl md:text-5xl font-display font-black text-gold mb-2\"\n                  initial={{ opacity: 0 }}\n                  whileInView={{ opacity: 1 }}\n                  transition={{ delay: index * 0.1 + 0.2 }}\n                  viewport={{ once: true }}\n                >\n                  {stat.value}\n                </motion.div>\n                <p className=\"text-white/60 text-sm font-semibold\">{stat.label}</p>\n              </motion.div>\n            ))}\n          </div>\n        </motion.div>\n      </div>\n    </section>\n  )\n}\n