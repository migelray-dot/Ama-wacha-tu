'use client'\n\nimport { motion } from 'framer-motion'\nimport Link from 'next/link'\nimport Image from 'next/image'\nimport { siteConfig } from '@/config/site'\n\nconst containerVariants = {\n  hidden: { opacity: 0 },\n  visible: {\n    opacity: 1,\n    transition: {\n      staggerChildren: 0.2,\n      delayChildren: 0.1,\n    },\n  },\n}\n\nconst itemVariants = {\n  hidden: { opacity: 0, y: 20 },\n  visible: {\n    opacity: 1,\n    y: 0,\n    transition: { duration: 0.8, ease: 'easeOut' },\n  },\n}\n\nexport function HeroSection() {\n  return (\n    <section className=\"relative w-full min-h-screen flex items-center justify-center overflow-hidden pt-20\">\n      {/* Animated Background */}\n      <div className=\"absolute inset-0 -z-10\">\n        <div className=\"absolute inset-0 bg-gradient-to-br from-dark-bg via-dark-secondary to-dark-bg\" />\n        \n        {/* Animated circles */}\n        <motion.div\n          className=\"absolute top-20 left-10 w-80 h-80 bg-gold/5 rounded-full blur-3xl\"\n          animate={{\n            x: [0, 50, -50, 0],\n            y: [0, 50, 0, -50],\n          }}\n          transition={{ duration: 20, repeat: Infinity }}\n        />\n        <motion.div\n          className=\"absolute bottom-20 right-10 w-96 h-96 bg-gold/3 rounded-full blur-3xl\"\n          animate={{\n            x: [0, -50, 50, 0],\n            y: [0, -50, 0, 50],\n          }}\n          transition={{ duration: 25, repeat: Infinity }}\n        />\n      </div>\n\n      {/* Content */}\n      <motion.div\n        className=\"container-custom text-center z-10 space-y-8\"\n        variants={containerVariants}\n        initial=\"hidden\"\n        animate=\"visible\"\n      >\n        {/* Logo */}\n        <motion.div\n          className=\"flex justify-center\"\n          variants={itemVariants}\n        >\n          <div className=\"w-32 h-32 relative drop-shadow-2xl\">\n            <Image\n              src=\"/logo.svg\"\n              alt=\"Ama Wacha Tu Logo\"\n              width={128}\n              height={128}\n              priority\n              className=\"w-full h-full object-contain\"\n            />\n          </div>\n        </motion.div>\n\n        {/* Main Headline */}\n        <motion.h1\n          className=\"font-display text-5xl md:text-6xl lg:text-7xl font-black leading-tight\"\n          variants={itemVariants}\n        >\n          Real Conversations.\n          <br />\n          <span className=\"gradient-text\">Real Stories.</span>\n          <br />\n          Real People.\n        </motion.h1>\n\n        {/* Subheading with tagline */}\n        <motion.div className=\"space-y-4\" variants={itemVariants}>\n          <p className=\"text-lg md:text-xl text-white/70 max-w-2xl mx-auto leading-relaxed\">\n            Every conversation starts somewhere.\n            <br />\n            Sometimes the best response is simply...\n          </p>\n          <motion.p\n            className=\"text-3xl md:text-4xl font-display font-bold text-gold italic\"\n            animate={{ opacity: [0.6, 1, 0.6] }}\n            transition={{ duration: 3, repeat: Infinity }}\n          >\n            Oh well....\n          </motion.p>\n        </motion.div>\n\n        {/* CTA Buttons */}\n        <motion.div\n          className=\"flex flex-col sm:flex-row gap-4 justify-center pt-8\"\n          variants={itemVariants}\n        >\n          <Link\n            href={siteConfig.links.youtube}\n            target=\"_blank\"\n            rel=\"noopener noreferrer\"\n            className=\"btn-primary inline-flex items-center justify-center gap-2 group\"\n          >\n            <span>▶</span>\n            Watch on YouTube\n          </Link>\n          <Link\n            href={siteConfig.links.spotify}\n            target=\"_blank\"\n            rel=\"noopener noreferrer\"\n            className=\"btn-secondary inline-flex items-center justify-center gap-2 group\"\n          >\n            <span>♫</span>\n            Listen on Spotify\n          </Link>\n        </motion.div>\n\n        {/* Secondary CTA */}\n        <motion.div variants={itemVariants}>\n          <Link\n            href=\"#episodes\"\n            className=\"text-white/60 hover:text-gold transition-colors inline-flex items-center gap-2 group\"\n          >\n            Explore Episodes\n            <motion.span\n              animate={{ x: [0, 5, 0] }}\n              transition={{ duration: 2, repeat: Infinity }}\n            >\n              →\n            </motion.span>\n          </Link>\n        </motion.div>\n      </motion.div>\n\n      {/* Scroll indicator */}\n      <motion.div\n        className=\"absolute bottom-10 left-1/2 -translate-x-1/2\"\n        animate={{ y: [0, 10, 0] }}\n        transition={{ duration: 2, repeat: Infinity }}\n      >\n        <div className=\"w-6 h-10 border-2 border-gold rounded-full flex justify-center p-2\">\n          <div className=\"w-1 h-2 bg-gold rounded-full\" />\n        </div>\n      </motion.div>\n    </section>\n  )\n}\n